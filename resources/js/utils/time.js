import moment from "moment"

export const secondsTillMidnight = () => {
    // let now = moment()
    // let endOfDay = now.clone().endOf('day')

    // return now.diff(endOfDay, 'seconds')

    return 4
}